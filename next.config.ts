import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    /* config options here */
    reactCompiler: true,
    // basePath: "/portofolio/show/test-sharing-vision-nextjs",
    // assetPrefix: "/portofolio/show/test-sharing-vision-nextjs",
};

export default nextConfig;
